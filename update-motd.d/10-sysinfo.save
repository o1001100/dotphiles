#!/bin/bash

# make a little space
printf "\n \n"

# get load averages
IFS=" " read LOAD1 LOAD5 LOAD15 <<<$(cat /proc/loadavg | awk '{ print $1,$2,$3 }')
# get free memory
IFS=" " read USED AVAIL TOTAL <<<$(free -htm | grep "Mem" | awk {'print $3,$7,$2'})
# get processes
PROCESS=`ps -eo user=|sort|uniq -c | awk '{ print $2 " " $1 }'`
PROCESS_ALL=`echo "$PROCESS"| awk {'print $2'} | awk '{ SUM += $1} END { print SUM }'`
PROCESS_ROOT=`echo "$PROCESS"| grep root | awk {'print $2'}`
PROCESS_USER=`echo "$PROCESS"| grep -v root | awk {'print $2'} | awk '{ SUM += $1} END { print SUM }'`
# get model name
NAME=`grep "Model" /proc/cpuinfo | cut -d ' ' -f2- | awk {'print $0'} | head -1`
# get cpu temp
tmp=$(</sys/class/thermal/thermal_zone0/temp)
CPUTEMP=$((tmp/1000)) 

W="\e[1;37m"
G="\e[1;32m"
R="\e[0;31m"

if [[ $CPUTEMP > 79 ]]
then
  TEMPCOLOUR=$R
else
  TEMPCOLOUR=$G
fi

echo -e "
    ${W}Running on: $NAME


$W    System Info:
$W        > Distro .............  $W`cat /etc/*release | grep "PRETTY_NAME" | cut -d "=" -f 2- | sed 's/"//g'`
$W        > Kernel .............  $W`uname -sr`
$W        > Uptime .............  $W`uptime -p`
$W        > Load ...............  $G$LOAD1$W (1m), $G$LOAD5$W (5m), $G$LOAD15$W (15m)
$W        > Processes ..........  $G$PROCESS_ROOT$W (root), $G$PROCESS_USER$W (user), $G$PROCESS_ALL$W (total)
$W        > CPU Temperature ....  $TEMPCOLOUR$CPUTEMP$W°C"


$W        > Memory .............  $G$USED$W used, $G$AVAIL$W avail, $G$TOTAL$W total$W
